- hosts: groupenyss
  roles:
    - viasite-ansible.apache-vhosts
  vars:
    apache_vhosts_sites:
      alacam.be:
        servername: "www.alacam.be"
        serveralias: "alacam.be"
        documentroot: "/var/www/html/alacam.be"
        #extra_parameters: |
         # RewriteCond %{HTTP_HOST} !^www\. [NC]
         # RewriteRule ^(.*)$ http://www.%{HTTP_HOST}%{REQUEST_URI} [R=301,L]        