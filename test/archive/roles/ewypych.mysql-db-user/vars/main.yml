---
# vars file for create-db-user
databases:
  testowadb:
    name: testdbansible
    user: adm_test
    userhost: localhost
    password: "{{ db_pass_test }}"
  seconddb:
    name: it360-db
    user: it360
    userhost: localhost
    password: "{{ db_pass_second }}"
